--- a/setup.cfg
+++ b/setup.cfg
@@ -43,7 +43,6 @@ install_requires =
   cffi >= 1.1.0
 tests_require =
   pytest-cov
-  pytest-flake8
   pytest-runner
   numpy
 python_requires = >= 3.6
@@ -61,7 +60,6 @@ doc =
 test =
   pytest-runner
   pytest-cov
-  pytest-flake8
 xcb =
   xcffib >= 0.3.2
 
@@ -73,7 +71,6 @@ build-dir = docs/_build
 test = pytest
 
 [tool:pytest]
-addopts = --flake8
 norecursedirs = build dist .cache .eggs .git
 
 [flake8]
